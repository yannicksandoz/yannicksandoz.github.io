{
  "name": "galerie-3d",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Galerie d'art 3D immersive, statique et modulaire (Vite + Three.js + Web Audio)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "assets": "node scripts/generate-assets.mjs",
    "test": "node scripts/test-commands.mjs && node scripts/test-schema.mjs && node scripts/test-voxel.mjs"
  },
  "dependencies": {
    "three": "0.166.1"
  },
  "devDependencies": {
    "vite": "^5.3.4"
  }
}
